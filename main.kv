<Container>:
    display: display
    rows: 1
    BoxLayout:
        orientation: "vertical"
        PgcdButton:
            on_release: root.pgcd()
        LeaveButton:
            on_release: root.leave()
    Label:
        id: display
        font_size: dp(50)
        text: '0'